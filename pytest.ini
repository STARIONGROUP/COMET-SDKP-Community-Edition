[pytest]
minversion = 9.0
testpaths = tests
 
python_files = test_*.py
python_classes = Test*
python_functions = test_*
 
addopts =
    -ra
    --strict-markers
 
markers =
    unit: fast unit tests with no external dependencies
    integration: tests requiring native libraries or external systems
    smoke: basic import and startup tests